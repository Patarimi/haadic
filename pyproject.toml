[project]
name = "haadic"
version = "0.6.0"
description = "A package for parametric layout design."
authors = [{name = "Patarimi", email="<38954040+Patarimi@users.noreply.github.com>"}]
readme = "docs/README.md"
requires-python = ">3.11,<3.14"
dependencies = [
    "ciel>=2.2.0",
    "cyclopts>=3.22.3",
    "klayout>=0.30.2",
    "lark>=1.2.2",
    "matplotlib>=3.10.3",
    "numpy>=2.3.2",
    "pandas>=2.3.1",
    "pydantic>=2.11.7",
    "pyqt6>=6.11.0 ; sys_platform == 'linux'",
    "python-dotenv>=1.1.1",
    "pyyaml>=6.0.2",
    "rich>=13.9.4",
    "scikit-rf>=1.8.0",
    "scipy>=1.16.1",
    "tabulate>=0.9.0",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""

[project.scripts]
haadic = "haadic.main:app"

[tool.poe.tasks]
update_show = "uv lock --dry-run --upgrade"
update_all = "uv sync --upgrade --all-groups --all-extras"
test = 'pytest --basetemp=tmp'
cover = "pytest --basetemp=tmp --cov-report term-missing:skip-covered --cov=haadic"
publish = "poetry publish --build --username __token__ --password $PYPI_TOKEN"
doc = "mkdocs build"

[tool.poe.tasks.lint]
sequence = [
    {cmd = "uvx ruff format"},
    {cmd = "uvx ruff check"},
    {cmd = "uvx ty check"},
]

[tool.poe.tasks.precommit]
sequence = [
    {ref = "update_all"},
    {ref = "lint"},
    {ref = "doc"},
    {ref = "cover"}
]

[tool.ruff]
extend-include = ["./workdir/*.py"]

[tool.pytest.ini_options]
testpaths = ["tests"]
log_cli_level = "ERROR"
log_file_level = "INFO"
log_file = "./pytest.log"

[dependency-groups]
dev = [
    "poethepoet>=0.36.0",
    "pytest>=8.4.1",
    "pytest-cov>=7.1.0",
]
doc = [
    "cyclopts[mkdocs]>=4.10.2",
    "mkdocs>=1.6.1",
    "mkdocs-api-autonav>=0.4.0",
    "mkdocs-material>=9.6.16",
]


[tool.ty.src]
exclude = ["haadic/io/wrappers/oems.py", "haadic/io/wrappers/ngsolve_w.py"]
